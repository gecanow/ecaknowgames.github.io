block main 
    link(rel='stylesheet', href='/styles/contact-style.css')
    .GeneralBackPic.WorkStation
        .main.u-textCenter.u-position.textOnImg
            h1.CenterTitle LET'S GET IN TOUCH!
            form(action='/send_email', method='POST')
                .InputContainer
                    input(type='text', name='name', value='', required='true', placeholder='Name*').Short
                    input(type='text', name='email', value='', required='true', placeholder='Email*').Short
                    input(type='text', name='subject', value='', required='true', placeholder='Subject*').Short
                    textarea(type='text', name='message', value='', required='true', placeholder='Message*').Large
                    button(type='submit', value='submit').SendButton Send
    script.
        const o = !{JSON.stringify(outcome)};
        if (o) {
            alert(o);
        }